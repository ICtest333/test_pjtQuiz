<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.pjt.quiz"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="kr.pjt.quiz.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="QuizActivity"></activity>
        <activity android:name="SearchActivity"></activity>
        <activity android:name="JavaActivity"></activity>
        <activity android:name="SqlActivity"></activity>
        <activity android:name="AndroidActivity"></activity>
        <activity android:name="JspActivity"></activity>
        <activity android:name="JsActivity"></activity>
        <activity android:name="EtcActivity"></activity>
        <activity
            android:name="kr.pjt.quiz.ResultActivity"
            android:label="@string/title_activity_result" >
        </activity>
    </application>

</manifest>
